// using HashStringToGuid;

// string originalString = "Hello";

// Optionally, pick your customize algorithm. You can supply your own hash too.
// StringToGuid stringToGuid = StringToGuid.Instance.WithCustomHash(HashAlgorithm.XxHash128);

// Console.WriteLine(StringToGuid.Instance.GetGuid(originalString));